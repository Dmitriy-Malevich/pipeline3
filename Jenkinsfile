properties([pipelineTriggers([githubPush()])])
pipeline {
    agent any

    stages {
        stage('1-Build') {
           steps {
               echo  "Start of Stage Build"
 	       sh "pwd"
               echo  "Building.............."
               echo  "End of Stage Build"
    }
}
}
